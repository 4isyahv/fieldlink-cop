{
  "name": "fieldlink-cop",
  "version": "1.0.0",
  "private": true,
  "description": "A deployable realtime common operating picture for distributed teams.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
